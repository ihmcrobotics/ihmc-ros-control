<launch>

    <!-- Load joint controller configurations from YAML file to parameter server -->
    <!--<rosparam file="$(find val_deploy)/config/val_ros_control_test.yaml" command="load"/>-->
    <rosparam file="$(find ihmc_ros_control)/config/val_java_param.yaml" command="load"/>

    <!-- load the controllers -->
    <node name="controller_spawner" pkg="controller_manager" type="spawner" respawn="false"
          output="screen" args="ihmc_valkyrie_control_java_bridge
                                          "/>
</launch>
